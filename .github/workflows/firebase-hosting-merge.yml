# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: cd client/lightsoutTwo && npm install
      - run: cd client/lightsoutTwo && npm run build
      - run: ls -la
      - run: ls -la client/lightsoutTwo/dist
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_TTLO_TWO }}
          channelId: live
          projectId: ttlo-two
          entryPoint: client/lightsoutTwo
